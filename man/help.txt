CLI for SNAP dataset, which is a collection of more than 50 large networks.

${commands}

If you need command details, use `snap-data help <command>`.
Or else see https://github.com/nodef/snap-data.sh.
